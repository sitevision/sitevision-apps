{
  "name": "@sitevision/api",
  "version": "0.0.4",
  "author": "Sitevision AB",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./server/*": "./dist/server/*.cjs",
    "./client/*": "./dist/client/*.cjs",
    "./common/*": "./dist/common/*.cjs"
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "rimraf dist/ && babel src --out-dir dist --out-file-extension .cjs --copy-files",
    "deploy": "echo 'this is lazy workaround'"
  },
  "devDependencies": {
    "@babel/cli": "^7.14.3",
    "rimraf": "^3.0.2"
  }
}
