import * as React from 'react';
{{#unless widget}}
  import styles from './App.scss';
{{/unless}}

export interface AppProperties {
  message: string;
  name: string;
}

const App: React.FunctionComponent<AppProperties> = ({ message, name }) => {
  return (
  {{#if widget}}
    <div className="env-dashboard-widget env-p-around--large">
      <h3 className="env-ui-text-sectionheading">{message} {name}</h3>
    </div>
  {{else}}
    <div className={styles.container}>
      <p className={styles.text}>
        {message} {name}
      </p>
    </div>
  {{/if}}
  );
};

export default App;
